"""Relay — a minimal messaging + community service with an AI assistant.

This package ships CORRECT (05-gallery.md §0.1): ``src/`` contains only correct
code; the bug gallery's naive variants live under ``tests/naive`` and are
injected through FastAPI ``app.dependency_overrides`` — the same seam the
harness uses — scoped to a single test.
"""
