// Idiom-pass linter stub (Task 1.2). Replaced by the real flat config at
// Task 3.1 when NestJS + typescript-eslint are installed and version-locked:
// - typescript-eslint strictTypeChecked + stylisticTypeChecked
// - eslint-plugin-import ordering
// - Nest-specific conventions (providers/modules naming)
// Kept as `.stub.mjs` so nothing half-configured executes before 3.1.
export default [];
